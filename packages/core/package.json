{
  "name": "@terouva/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Logique métier pure de Terouva (scoring, génération de messages, parsing email d'alerte, types) — partagée par l'app desktop (Tauri) et l'app web local-first. Aucune dépendance runtime, aucun accès I/O.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
